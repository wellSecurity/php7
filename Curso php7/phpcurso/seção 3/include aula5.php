<?php

function somar($a, $b) {
    return $a + $b;
}

/*
require(ele obriga que o arquivo esteja funcionando ou existindo, caso que não ele para o serviço)

include(ele tenta rodar mesmo que o arquivo não esteja funcionando apenas aparecendo uma msg de erro)

de preferencia use o require, porque o include mesmo sendo util ele precisa se configurado e acaba ocorrendo muitas falhas
*/
?>